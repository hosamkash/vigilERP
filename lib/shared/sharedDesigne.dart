import 'package:flutter/material.dart';

class sharedDesigne {
  static const Color primaryColor = Colors.purple;
  static const Color defaultColorFont = Colors.black;
  static ColorScheme colorScheme = ColorScheme.fromSeed(seedColor: primaryColor);

  static TextStyle StyleBigHeader = const TextStyle(
    fontWeight: FontWeight.bold,
    color: Colors.black,
    fontSize: 15,
  );



}
